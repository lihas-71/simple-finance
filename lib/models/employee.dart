class Employee {
  final String name;
  final double baseSalary;
  final double bonus;
  final String? id;
  final bool isActive;

  Employee(this.name, this.baseSalary, this.bonus, this.isActive, {this.id});
}
